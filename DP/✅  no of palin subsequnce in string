
# this is the template for the problem
public static void no_of_plain_sub(String s)
{
  int dp[][]= new int[l][l];
  for(int i=0;i<l;i++)
  {
    dp[i][i]=1;
  }
  for(int i=0;i<l-1;i++)
  {
    if(s.charAt(i)==s.charAt(i+1))
    dp[i][i+1]= 3;
    else
    dp[i][i+1]=2;
  }
  int len=3;
  while(len<l)
{
 for(int i=0;i<l-len+1;i++)
{
  int x= i;
int y= i+len-1;
if(s.charAt(x)==s.charAt(y))
{
 dp[x][y]= dp[x+1][y]+ dp[x][y-1]- dp[x+1][y-1]+ dp[x+1][y-1]+1;
}
else
{
  dp[x][y]= dp[x+1][y]+ dp[x][y-1]- dp[x+1][y-1];
}

}
len++;
}
  return dp[0][l-1];
}  
